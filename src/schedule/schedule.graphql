scalar DateTime

type Query {
  now: DateTime!
  getEvent: [ScheduledEvent!]!
}

type Mutation {
  processFragment(html: String!): ScheduledEvent!
}

type ScheduledEvent {
  id: ID!
  title: String!
  begin: DateTime!
  end: DateTime!
}
